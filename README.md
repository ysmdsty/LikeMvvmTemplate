# How To

### Carthage

```
carthage update
=
carthage update --configuration Release --platform all --verbose false --checkout true --build true
```

carthage update --platform iOS

### git

git submodule update --init
